---
description: 커밋 준비 전 빌드 사전 점검
alwaysApply: true
---

# 커밋 준비 사전 점검

- **"커밋 준비"** 요청 시, 스테이징·메시지 제안 **전에** 저장소 루트에서 `npm run build` 실행
- 빌드 실패 시 커밋 준비 중단 → 오류 공유 후 수정·재빌드
- 빌드 성공 후에만 `git add`, `git status`, 커밋 메시지 제안 진행
- 평상시 불필요한 반복 빌드는 하지 않는다
